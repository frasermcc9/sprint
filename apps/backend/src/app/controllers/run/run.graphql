type Mutation {
  createRun(
    token: String!
    startDate: String!
    endDate: String!
    startTime: String!
    endTime: String!
  ): Run
}

type Run {
  userId: String
  date: String
  duration: Int
  heartRate: [Int]
  vo2max: Int
  intensityFeedback: Int
}
